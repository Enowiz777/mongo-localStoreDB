extends base.pug

block head
    title Wetube
block content
    div.headerText
        h1 Edit an item (UPDATE)
    if item 
        form(action=`/${item.id}/edit` method="POST")
            div
                label(for='html') Title
                input(placeholder=`${item.title}`, required, type="text", name="title")
            div
                label(for='html') Manufacturer
                input(placeholder=`${item.manufacturer}`, required, type="text", name="manufacturer")
            div
                label(for='html') Distributor
                input(placeholder=`${item.distributer}`, required, type="text", name="distributer")
            div
                label(for='html') Description
                input(placeholder=`${item.description}`, required, type="text", name="description")
            div
                label(for='html') Price
                input(placeholder=`${item.price}`, required, type="text", name="price")
            div 
                label(for='html') Unit
                input(placeholder=`${item.count}`, required, type="number", name="count")
            div 
                label(for='html')
                input(type="submit", value="Update Item")